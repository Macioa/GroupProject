<section>
  <h2>List of Events</h2>
        <ul>
          <% for(let i = 0; i < events.length; i++){ %>
            <li>
              <a href="/events/<%=events[i]._id%>"><%=event[i].name%></a>
            </li>
          <% } %>
        </ul>
      </main>
<section>
